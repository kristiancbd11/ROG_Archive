<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo del EditText -->
    <solid android:color="#F2784B"/>

    <!-- Borde exterior -->
    <stroke
        android:color="#bd463a"
        android:width="2dp" />

    <!-- Borde interior (más delgado) -->
    <stroke
        android:color="#8C342B"
        android:width="2dp"
        android:dashGap="2dp" />
</shape>
